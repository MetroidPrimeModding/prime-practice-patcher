#ifndef PRIME_PRACTICE_PATCHER_PATCHER_HPP
#define PRIME_PRACTICE_PATCHER_PATCHER_HPP

#include <vector>
#include <string>

int patch_iso(std::vector<std::string> args);

#endif //PRIME_PRACTICE_PATCHER_PATCHER_HPP
